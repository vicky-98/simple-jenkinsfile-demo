pipeline{
	agent any
	
		stages{
			
			stage("init"){
				steps{
					echo "initial"
				}
			}
			
			stage("build"){
				steps{
					echo "build success"
				}
			}
			
			stage("deploy"){
				steps{
					echo "deployed the build"
				}
			}
		}
	}